import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rohitiyer.endlesschord',
  appName: 'Endless Chord',
  webDir: 'build'
};

export default config;
